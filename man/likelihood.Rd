% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/log-likelihood.R
\name{likelihood}
\alias{likelihood}
\title{Compute Log-Likelihood for Multiple Extreme‐Value Models}
\usage{
likelihood(data, model, theta)
}
\arguments{
\item{data}{Numeric vector of observations. NaN values are removed before computation.}

\item{model}{Character string giving the model code.
The first three letters specify the distribution:
’GUM’, ’NOR’, ’LNO’, ’GEV’, ’GLO’, ’GNO’, ’PE3’, ’LP3’, or ’WEI’.
A five or six-character code adds a covariate signature:
’10’/\code{100} = trend in location, ’11’/\code{110} = trends in location and scale.}

\item{theta}{Numeric parameter vector for the specified model and signature.
From the parameter vector, we determine:
\itemize{
\item u (location).
\item s (scale).
\item k (shape) for three-parameter distributions.
\item Additional trend coefficients if there is non-stationarity.
}}
}
\value{
A single numeric value: the sum of pointwise log-densities.
Returns -Inf if any transformation term is non-positive or parameters are NaN.
}
\description{
Calculates the total log-likelihood of a data vector under various
extreme-value distributions: GUM, NOR, LNO, GEV, GLO, GNO PE3, LP3, and WEI.
Allows for optional linear covariate trends in location and/or scale.
Returns -Inf whenever parameters or data violate distribution support.
}
\details{
\enumerate{
\item Removes NaN values from data and builds a covariate scaled on \code{[0,1]}.
\item Parses the stationary or non-stationary signature to compute u and s.
\item Adds k for three-parameter families and reparameterizes PE3/LP3 when needed.
\item Computes the log-density for each supported distribution, replacing invalid or NaN terms with -Inf.
\item Sums the pointwise log-likelihoods for output.
}
}
