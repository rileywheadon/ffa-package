% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generalized-likelihood.R
\name{generalized.likelihood}
\alias{generalized.likelihood}
\title{Compute Generalized Log‐Likelihood for GEV Models with Beta Prior}
\usage{
generalized.likelihood(data, model, theta, prior)
}
\arguments{
\item{data}{Numeric vector of observations (e.g., annual maxima).
Any \code{NaN} values are removed prior to likelihood computation.}

\item{model}{Character string specifying the model form.
One of \code{'GEV'}, \code{'GEV100'}, or \code{'GEV110'}.}

\item{theta}{Numeric vector of GEV parameters:
\itemize{
\item For \code{'GEV'}    : \verb{theta = (mu, sigma, kappa)}
\item For \code{'GEV100'} : \verb{theta = (mu1, mu2, sigma, kappa)}
\item For \code{'GEV110'} : \verb{theta = (mu1, mu2, sigma1, sigma2, kappa)}
Trends are applied as \code{mu = mu1 + (covariate * mu2)} and/or
\code{sigma = sigma1 + (covariate * sigma2)}, where \code{covariate} is
scaled linearly on \code{[0,1]} over the sample.
}}

\item{prior}{Numeric vector of length 2 giving the Beta prior parameters
\verb{(p, q)} on the shape parameter kappa.}
}
\value{
A single numeric value: the sum of the pointwise log‐likelihoods of the data
under the specified GEV model plus the log‐density of the Beta prior on kappa.
Returns \code{-Inf} if likelihood is zero.
}
\description{
Calculates the log‐likelihood of a sample under a Generalized Extreme Value
(GEV) distribution (stationary or with covariate trends) combined with a
Beta(p, q) prior on the shape parameter. Supports three model forms:
\itemize{
\item \code{'GEV'}   : stationary location, scale, and shape
\item \code{'GEV100'}: linear trend in location only
\item \code{'GEV110'}: linear trends in location and scale
}
}
\details{
\enumerate{
\item Removes \code{NaN} values from \code{data} and constructs a covariate vector of
the same length, linearly spaced on \code{[0,1]}.
\item Unpacks \code{theta} into \verb{(mu, sigma, kappa)} or trend variants depending on \code{model}.
\item Computes the GEV log‐density for each data point.
\item Computes the log‐density of the Beta(p, q) prior on kappa.
\item Returns the total log‐likelihood (the sum of steps 3 and 4).
}
}
