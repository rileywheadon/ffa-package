% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gmle-estimation.R
\name{gmle.estimation}
\alias{gmle.estimation}
\title{Perform Generalized MLE for GEV Models with Trend and Prior}
\usage{
gmle.estimation(data, model, prior, years)
}
\arguments{
\item{model}{Character string specifying the GEV model form:
’GEV’   = stationary,
’GEV100’ = trend in location only,
’GEV110’ = trends in location and scale.}

\item{prior}{Numeric vector of length 2 giving the Beta prior parameters
(p, q) on the shape parameter kappa.}

\item{df}{Dataframe with columns "max", a vector of annual maxima observations,
and "year", a vector of years corresponding to the observations in "max". Any
\code{NaN} values are removed prior to likelihood computation.}
}
\value{
A list with components:
\itemize{
\item params: Numeric vector of the estimated GEV parameters in the same order as
used by generalized.likelihood().
\item mll   : The maximized log‐posterior (i.e. log‐likelihood plus log‐prior).
}
}
\description{
Fits a Generalized Extreme Value (GEV) model—stationary or with linear trends
in location and/or scale—by maximizing the posterior log‐likelihood (MLE with
Beta prior on the shape). Uses L‐moment estimates for initialization and
nlminb for constrained optimization.
}
\details{
\enumerate{
\item Calls lmom.estimation() on the cleaned data (removes NaN) to obtain initial
estimates of (mu, sigma, kappa).
\item Sets initial trend parameters to 0 for non‐stationary models.
\item Defines lower and upper bounds to enforce sigma > 0 and |kappa| < 0.5.
\item Constructs an objective function that returns the negative of
generalized.likelihood().
\item Runs nlminb() with box constraints to find the posterior mode.
}
}
